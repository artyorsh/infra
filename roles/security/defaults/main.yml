---
# See templates/iptables.conf
security_firewall_allow_rules:
  - desc: "Allow HTTP"
    proto: "tcp"
    port: "80"

  - desc: "Allow HTTPS"
    proto: "tcp"
    port: "443"

  - desc: "Allow SSH"
    proto: "tcp"
    port: "{{ security_ssh_port }}"
