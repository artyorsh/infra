---
- hosts: "vpnhost"
  become: true

  roles:
    - role: "system"
      tags:
        - "system"

    - role: "security"
      tags:
        - "security"

    - role: "docker"
      tags:
        - "docker"

    - role: "cloudflare"
      tags:
        - "cloudflare"

    - role: "wireguard"
      tags:
        - "wireguard"
