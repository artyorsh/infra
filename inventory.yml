---
all:
  hosts:
    medialab:
      ansible_ssh_port: "{{ security_ssh_port }}"
      ansible_ssh_private_key_file: "~/.ssh/id_rsa"

    vpnhost:
      ansible_ssh_port: "{{ security_ssh_port }}"
      ansible_ssh_private_key_file: "~/.ssh/id_rsa"